
---

- hosts: all
  become: true
#  gather_facts: false     ##lock/unlock if needed.
  tasks:

  - name: install apache and php packages
    package:
      name:
        - "{{ apache_package }}"
        - "{{ php_package }}"
      state: latest
      update_cache: yes

